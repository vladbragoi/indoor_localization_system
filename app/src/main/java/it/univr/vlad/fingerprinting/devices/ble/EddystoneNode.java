package it.univr.vlad.fingerprinting.devices.ble;

class EddystoneNode extends BleNode {

    EddystoneNode(String id, int value, String uid, String timestamp) {
        super(id, value, uid, timestamp);
    }

}